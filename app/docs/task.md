[DONE]请将三个模型的pytorch代码模型定义以及底层的训练算法细节形成文档保存到app/docs/model-struct.md;将当前我们使用每日坐席录音进行VAD语音拆分、声纹相似度计算并标记说话人，然后使用标记好的说话人声纹对三个模型进行增量训练并对比新模型效果的方法论以及相关代码和工具使用方法保存到app/docs/model-opt.md文档


[DONE]训练模块设计文档我已经挪到app/docs/voiceprint-training-design.md;请对该方案进行调整：
-1、新增一个api接口供客服和催收两个业务系统实时调用以推送通话录音信息，该接口中含有坐席ID、客户号码、时间戳等元数据，以及通话录音信息（可以是直接二进制流、录音文件url或录音ID），该接口在本地sqlite数据库汇总记录该录音信息，并根据参数将录音文件保存到本地录音保存目录；
-2、新增一个录音文件预处理模块，该模块在命令行调起来后从sqlite数据库获取未处理的录音清单，并循环进行训练前录音处理（如通话者拆分，VAD检测、噪音处理等），该模块会在sqlit
e数据库汇总记录每个录音文件预处理的状态和结果；
-3、模型增量训练模块，从sqlite数据库中读取还没有预训练的录音及其预处理文件然后进行增量训练，并对新模型进行评估，如果效果超过上一版本效果则把新模型元数据信息保存到sqlite数据库，并将新模型保存到api/models/目录供api接口服务使用。

[DONE] 优化坐席上传录音页面，录音文件名格式支持两种：{坐席ID}-{客户ID}-{MMDDHHMMSS}.xxx，和{客户ID}-{MMDDHHMMSS}.xxx，对于后者系统应自动使用当前坐席的ID向后台接口推送坐席信息

[DONE]优化模型管理员界面：1、将“批量预处理”菜单文字改为“录音断句”，点击该菜单后，界面显示后台进行VAD断句使用的算法及其参数，模型管理员可对算法和参数进行设置，确认后点击“开始断句”按钮后后台实际执行各录音的断句操作；2、系统默认对已上传未断句的录音进行断句，但界面也可以让模型管理选择对部分录音进行断句，筛选的维度有上传录音的坐席号、某个客户ID、上传时间范围。3、界面能够显示每个录音被断句后的结果，每个录音点击后可查看断开的每个片段，点击片段可以在浏览器播放录音片段，模型管理员可选择将某个片段设置为“忽略”状态，被忽略后该片段不参与后续的“说话人打标”流程。4、录音文件可以被重断句，最新的断句结果不会覆盖之前的断句结果。

[DONE]优化模型管理员界面的“说话人打标”功能：对于每个被断句后的录音，模型管理员可以选择使用“人工打标”和“系统打标”两种模式对每个录音片段进行说话人代表。在“人工打标”界面，模型管理员可以为每个录音片段人工打标为某个存在的说话人（坐席ID或客户ID或任意其他说话人），或打标为某个系统还未记录的说话人（此时需记录该新说话人的ID和备注）。在“系统打标”界面，系统会显示自动打标的算法及其参数，模型管理员可以手工调整相关参数后再开始系统自动打标流程，模型管理员可对系统打标的结果进行手工调整并保存最终的打标结果。

[DONE]优化模型管理员界面的”模型增量训练“功能：对于每个模型及其增量训练版本，系统会记录模型checkpoint时间戳，系统会根据时间戳自动计算每个模型最新checkpoint之后系统又新增了哪些说话人及其录音记录，或已有说话人的新录音记录。当新说话人和录音片段数量达到一定阈值后，模型管理员可选择使用这些新增的录音对某个模型checkpoint进行增量训练，并使用新生成的checkpoint对存量说话人及其录音进行打分，并将打分结果与历史版本的打分结果进行对比，并给出是否使用新checkpoint替换本模型老版本的建议。

[DONE]在模型管理员界面增加”模型详情“按钮，点击后以TreeView形式展示SQLite数据库中记录的模型及其各个checkpoint清单，点击Treeview中某个模型的节点时，界面右边区域以图形化形式展示该模型的pytorch定义，应尽可能形象化地展示该模型网络所定义的各个层，每个层的数学定义及其参数，当然也可以直接查看其详细Python代码。点击treeview中某个checkpoint节点后在“模型详情”区域显示该checkpoint训练所使用的声纹片段，该checkpoint对所有说话不同录音计算的embedding值，同一说话人不同录音打出的相似度评分清单，以及不同说话人之间录音的相似度分数。系统能根据当前所有模型checkpoint对所有说话人自己或相互之间录音相似度打分给出各模型checkpoint模型效果的排序。

[DONE]在模型管理员界面增加“模型发布”菜单，管理员可以根据系统建议或手工设定将某个模型的checkpoint指定为API 接口服务使用的底层模型，当管理员选中checkpoint并点击确认后，系统自动完成模型切换。


[DONE] API和WEB层要使用统一的python类以及统一的元数据信息，除了录音文件、VAD片段、模型checkpoint本身不保存到SQLite外，其他元数据信息都要进SQLite，不能依赖文件目录结构；请据此原则对app/api工程代码进行优化。

2、 WEB界面优化需求
--首页等各页面的系统名称改为“声纹管理系统”
--model-manager页面的图形化按钮菜单去掉，统一只使用页面顶层的菜单，依次为“首页”、“VAD参数”、“录音断句”、“说话人打标”、“增量训练”、“模型评估对比“、”模型管理”

3、WEB界面优化需求
--每个具体功能页面的菜单栏只显示当面的功能菜单名，以及返回首页的导航菜单

4、WEB界面优化需求
-模型管理员首页无需显示详细录音清单，改而显示系统运行时状态、以及关键要素的统计信息，具体如下：
--录音信息：系统中已上传未断句的录音数量、最近7日新上传的录音数量、已断句录音数量、总共多少声纹片段
--说话人信息：已经有多少打标的说话人，涉及多少声纹片段、还有多少片段未打标
--模型信息：系统总共有多少个模型，每个模型多少版本，还有多少个声纹片段未用于训练模型
--系统运行时信息：python进程已经运行时长、占用内存量、线程池大小

5、接口调用日志和首页统计
-为系统的声纹比对接口增加调用日志记录SQLite表，并记录每次调用详细信息（语音流只记录通过接口直接上传的原始录音，通过URL或 audio_id标识的，日志表中只记录URL或audio_id）
-在模型管理员和系统管理员首页增加当天和最近7天、最近30天的接口调用次数、响应时长分布统计信息


6、对声纹管理系统python WEB页面进行优化或调整
-model_manager首页的“说话人信息”区域增加“已打标片段“统计值
-model_manager/segments页面优化内容
--顶部的统计项调整为”总录音数“、”已断句“、”已打标“、”未断句"、“未打标”五个指标
--默认不要展示所有录音清单，而是在用户设定了筛选条件点击“筛选”按钮后才查询具体的录音文件，并且如果录音文件过多的话，要支持分页展示
--“开始断句”按钮要在用户选中了特定未断句录音后才激活可供用户启动断句处理
--点击“查看片段”进入“录音详情”页面中，每个片段的“操作“列下的按钮看不清，点击后显示”取消忽略“，再点击后看又是白色背景，看不到提示内容；”设置“按钮改为两个选项，选项一为设置为系统已有的某个说话人，提供下拉框供用户选择，选项二为让用户将该片段设置为某个新的说话人，用户此时需要输入新说话人的ID。
--录音详情页面选择”+重新断句“选项并点击”重新断句“后系统提示”启动失败，undefined"错误
-model_manager/label"说话人打标“页面
--”手动打标“功能实现有误，应支持让用户把每个片段打标到某个具体的说话人，比如具体的坐席号或客户号，或者标记为噪音（将被忽略），当前的手工打标只是片段打标为三种不同的类型，而没有到具体的说话人。
--“系统自动打标”应可以让用户选择使用的模型和checkpoint，每次只能选择一个模型及其checkpoint，系统应展示出每个片段自动打标结果的原因，例如片段与已有说话人的相似度分数，如果与现有说话都不类似，但未打标片段中有某几个之间两两相似度都很高，这几个片段被系统标记为候选的新说话人，特别是如果片段与原始录音的客户ID关联并且该客户ID还没有打标的说话人结果，则该几个片段便推荐打标为该客户ID的说话人声纹。系统自动打标结果需人工确认后才最终保存到SQLite数据库。
--如果用户对已经打好标的片段进行了调整，则该片段要被标记为已打标但未训练状态。SQLite中要记录每个打好标的片段被哪些模型引用进行了增量训练；这样变更的打标结果可以在后续用于再次训练模型。
--SQLite中也要记录每个片段被打标的历史日志，信息有：时间戳、确认用户ID、系统打标的原因（与已有说话人的相似度、与其他片段的相似度以及与已有不同说话人的最大相似度等）

7、对声纹管理系统python WEB页面进行优化或调整
-model-manager首页的“录音信息”区域将“待断句”调整为“待断句录音“并放到“已断句录音”指标后面，并且首页的”待断句“数量（0）与model-manager/segments页面中的”未断句“指标（5）不一致；
-model-manager/segments首页”已断句“调整为”已断句录音“，”未断句“调整为”未断句录音“并将该指标放到”已断句录音”后面，“已打标”改为“已打标片段”，“未打标”调整为“未打标片段”
-model-manager/segments中，坐席选项卡中唯一的坐席名称显示为乱码；客户选择框的提示文字改为”客户ID“，状态改为只有”已断句“和”未断句“、”无法断句“三种；对于未断句的录音，”查看片段“按钮设置为无法点击，只有”已断句“的录音才激活“查看片段”按钮
-model-manager/segments页面中，选中筛选出的录音后点击”开始断句“，系统弹出对话框提示”启动失败“，请修复该问题

8、对声纹管理系统python WEB页面进行优化或调整
-model-manager首页的"待断句数量"为0，与model-manager/segments页面中的“未断句录音“结果5不一致
-model-manager/segments中，坐席选项卡中唯一的坐席ID仍然显示为乱码
-model-manager/segments中显示”未断句录音“为5，但”状态“选项选中”未断句“点击”筛选“后结果清单为空
-model-manager/segments中，选中某个已断句的录音，点击“开始断句（选中后激活）”按钮，按钮状态一直显示“处理中”，看不到重新断句后的结果。这里系统应支持对同一录音进行重新断句，并且可以看到同一录音的多个不同版本断句结果

9、对声纹管理系统python WEB页面进行优化或调整
-model-manager首页的"待断句数量"为0，与model-manager/segments页面中的“未断句录音“结果5不一致
-segments中，选中某个已断句的录音，例如ID为50的录音，点击“开始断句（选中后激活）”按钮，两次弹窗后ID为50的录音没有出现在“已断句”录音筛选结果中

10、对说话人打标页面(model-manager/label)内容进行优化
-顶端的三个指标名称依次改为“总声纹片段”（要提出被标记为忽略的片段）、“已打标片段”、“待打标片段”
-系统自动打标功能下，界面至少能识别出系统自带的三个预训练模型及其checkpoint，当前界面没能列举出模型和版本
-系统应能够根据过往模型checkpoint的表现推荐出优先使用的模型
-系统通过模型自动执行说话人打标，应将涉及到的关键参数展示给用户，并可以让用户进行调整后再启动自动打标过程
-手工打标功能应复用model-manager/segments/<id>?batch=<batch_id>页面中手工对语音片段进行手工打标相同的功能
-页面不要默认列举系统中所有的语音片段，而是提供几个筛选维度，用户设置并筛选后再展示结果片段清单，要支持分页展示
-系统自动打标支持对用户筛选出的结果清单进行自动打标，也支持对系统中所有未打标的片段进行全量分析打标，但无论需要打标的片段是哪些，系统都会运用全量声音片段进行对比分析，以追求达到最佳的自动打标结果
-系统自动打标结果应展示出每个待打标片段的推荐打标结果，以及得到该结果的原因说明，例如与其他以打标声纹的相似度分数很高，或很低，以及被分到同一组并推荐标识为新的说话人等。