[DONE]请将三个模型的pytorch代码模型定义以及底层的训练算法细节形成文档保存到app/docs/model-struct.md;将当前我们使用每日坐席录音进行VAD语音拆分、声纹相似度计算并标记说话人，然后使用标记好的说话人声纹对三个模型进行增量训练并对比新模型效果的方法论以及相关代码和工具使用方法保存到app/docs/model-opt.md文档


[DONE]训练模块设计文档我已经挪到app/docs/voiceprint-training-design.md;请对该方案进行调整：
-1、新增一个api接口供客服和催收两个业务系统实时调用以推送通话录音信息，该接口中含有坐席ID、客户号码、时间戳等元数据，以及通话录音信息（可以是直接二进制流、录音文件url或录音ID），该接口在本地sqlite数据库汇总记录该录音信息，并根据参数将录音文件保存到本地录音保存目录；
-2、新增一个录音文件预处理模块，该模块在命令行调起来后从sqlite数据库获取未处理的录音清单，并循环进行训练前录音处理（如通话者拆分，VAD检测、噪音处理等），该模块会在sqlit
e数据库汇总记录每个录音文件预处理的状态和结果；
-3、模型增量训练模块，从sqlite数据库中读取还没有预训练的录音及其预处理文件然后进行增量训练，并对新模型进行评估，如果效果超过上一版本效果则把新模型元数据信息保存到sqlite数据库，并将新模型保存到api/models/目录供api接口服务使用。

[ ] 优化坐席上传录音页面，录音文件名格式支持两种：{坐席ID}-{客户ID}-{MMDDHHMMSS}.xxx，和{客户ID}-{MMDDHHMMSS}.xxx，对于后者系统应自动使用当前坐席的ID向后台接口推送坐席信息

[ ]优化模型管理员界面：1、将“批量预处理”菜单文字改为“录音断句”，点击该菜单后，界面显示后台进行VAD断句使用的算法及其参数，模型管理员可对算法和参数进行设置，确认后点击“开始断句”按钮后后台实际执行各录音的断句操作；2、系统默认对已上传未断句的录音进行断句，但界面也可以让模型管理选择对部分录音进行断句，筛选的维度有上传录音的坐席号、某个客户ID、上传时间范围。3、界面能够显示每个录音被断句后的结果，每个录音点击后可查看断开的每个片段，点击片段可以在浏览器播放录音片段，模型管理员可选择将某个片段设置为“忽略”状态，被忽略后该片段不参与后续的“说话人打标”流程。4、录音文件可以被重断句，最新的断句结果不会覆盖之前的断句结果。

[ ]优化模型管理员界面的“说话人打标”功能：对于每个被断句后的录音，模型管理员可以选择使用“人工打标”和“系统打标”两种模式对每个录音片段进行说话人代表。在“人工打标”界面，模型管理员可以为每个录音片段人工打标为某个存在的说话人（坐席ID或客户ID或任意其他说话人），或打标为某个系统还未记录的说话人（此时需记录该新说话人的ID和备注）。在“系统打标”界面，系统会显示自动打标的算法及其参数，模型管理员可以手工调整相关参数后再开始系统自动打标流程，模型管理员可对系统打标的结果进行手工调整并保存最终的打标结果。

[ ]优化模型管理员界面的”模型增量训练“功能：对于每个模型及其增量训练版本，系统会记录模型checkpoint时间戳，系统会根据时间戳自动计算每个模型最新checkpoint之后系统又新增了哪些说话人及其录音记录，或已有说话人的新录音记录。当新说话人和录音片段数量达到一定阈值后，模型管理员可选择使用这些新增的录音对某个模型checkpoint进行增量训练，并使用新生成的checkpoint对存量说话人及其录音进行打分，并将打分结果与历史版本的打分结果进行对比，并给出是否使用新checkpoint替换本模型老版本的建议。

[ ]在模型管理员界面增加”模型详情“按钮，点击后以TreeView形式展示SQLite数据库中记录的模型及其各个checkpoint清单，点击Treeview中某个模型的节点时，界面右边区域以图形化形式展示该模型的pytorch定义，应尽可能形象化地展示该模型网络所定义的各个层，每个层的数学定义及其参数，当然也可以直接查看其详细Python代码。点击treeview中某个checkpoint节点后在“模型详情”区域显示该checkpoint训练所使用的声纹片段，该checkpoint对所有说话不同录音计算的embedding值，同一说话人不同录音打出的相似度评分清单，以及不同说话人之间录音的相似度分数。系统能根据当前所有模型checkpoint对所有说话人自己或相互之间录音相似度打分给出各模型checkpoint模型效果的排序。

[ ]在模型管理员界面增加“模型发布”菜单，管理员可以根据系统建议或手工设定将某个模型的checkpoint指定为API 接口服务使用的底层模型，当管理员选中checkpoint并点击确认后，系统自动完成模型切换。